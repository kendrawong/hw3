//space between lines 5
//length added to lines 10

var counter = 0;
var length = 5;
	while(counter <= width){
    line(counter, 0, counter, length);
    counter = counter + 5;
    length = length + 10;
  }
